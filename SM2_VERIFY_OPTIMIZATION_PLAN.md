# SM2 验签优化方案

## 1. 背景与目标

当前 gm-java 的 SM2 验签已经完成 Java 侧输入校验、ZA 缓存、JNI 回退一致性以及 native 公钥预计算表复用等优化。最新 Windows x64、JDK 25、Bouncy Castle 1.84 对比结果如下：

| 实现 | 验签延迟 | 说明 |
|---|---:|---|
| gm-java | 约 53.6 us/op | 同一公钥热路径，native 可用 |
| BC | 约 46.1 us/op | lightweight signer 预初始化并复用 |
| 差距 | gm-java 慢约 15%–16% | 不同基准轮次会有少量波动 |

native 微基准结果：

| 场景 | 延迟 |
|---|---:|
| 同公钥热路径 | 约 52.3 us/op |
| 强制公钥缓存失效 | 约 61.7 us/op |
| 首次调用 | 约 77.1 us/op |
| 轮换 10–100 个公钥 | 约 65–67 us/op |

Profiling 表明，默认验签路径约 99% 的时间位于 native Shamir 双标量乘法。Java 端 ZA、SM3、数组复制和 JNI 边界已经不是主要瓶颈。

本方案的目标是：

1. 优先降低单签热路径延迟，争取从约 53.6 us/op 降至 46 us/op 左右。
2. 不以明显增加首次调用、缓存失效或多公钥轮换成本为代价。
3. 保持 JDK 8/21/25、native/Java fallback 和跨平台源码行为一致。
4. 所有 native 优化必须经过大规模差分测试，不接受仅凭局部微基准启用的实现。
5. 单项优化热路径收益低于 3%，或冷路径回退明显时，默认不合入。

## 2. 当前瓶颈

SM2 验签的核心计算为：

```text
t = (r + s) mod n
(x1, y1) = [s]G + [t]P
R = (e + x1) mod n
检查 R == r
```

当前 native 实现使用 wNAF Shamir 双标量乘法：

- 固定基点窗口：w=7，32 项预计算表。
- 变基公钥窗口：w=6，16 项线程本地预计算表。
- 主循环执行约 256 次 Jacobian 倍点及若干次 Jacobian/仿射混合加法。
- 循环结束后将 Jacobian 点 `(X, Y, Z)` 转为仿射点 `(x, y)`。

主要问题在于最终仿射转换：

```text
zInv = Z^(-1)
x = X * zInv^2
y = Y * zInv^3
```

当前模逆加法链约需要 297 次域平方和 17 次域乘法，而且验签最终只使用 `x`，仿射 `y` 的计算完全不参与结果判断。

## 3. 优化优先级

## 3.1 Phase 0：建立可归因的性能基线

在继续修改算法前，应先固化测量口径。

### 测量项

- 完整 `SM2Signature.verify` 延迟。
- `nativeVerifyCore` 延迟。
- Shamir 主循环延迟。
- 最终 Jacobian 转仿射延迟。
- Montgomery 乘法和平方的周期数。
- 分支预测失败、L1/L2 cache miss、指令数和 cycles/op。

### 工具

- JMH 或独立 fork 的固定基准程序。
- Windows Performance Recorder、Intel VTune 或 AMD uProf。
- GCC/Clang 反汇编输出。
- `perf stat`，用于 Linux x64 交叉验证。

### 基准场景

| 场景 | 目的 |
|---|---|
| 同一公钥持续验签 | 测量生产热路径 |
| 首次验签 | 覆盖固定基点表和公钥表初始化 |
| 每次强制 cache miss | 测量公钥预计算成本 |
| 轮换 2/10/50/100 个公钥 | 测量多租户和网关场景 |
| 32B/1KB/4KB 消息 | 区分椭圆曲线与摘要成本 |
| 单签和 batch 4/8/16/32 | 区分延迟与吞吐优化 |

Phase 0 不改变生产代码，只产出可复现基线和热点报告。

## 3.2 Phase 1：使用 Jacobian 项目坐标直接完成验签比较

这是本方案中优先级最高、预期收益最大的优化。

### 数学依据

Shamir 计算结束后得到 Jacobian 点：

```text
x_affine = X / Z^2 mod p
```

验签要求：

```text
(e + x_affine) mod n == r
```

令：

```text
x0 = (r - e) mod n
```

因为 SM2 曲线满足：

```text
n < p < 2n
```

且规范仿射横坐标满足 `0 <= x_affine < p`，所以满足 `x_affine mod n == x0` 的候选最多只有两个：

```text
x_candidate = x0
x_candidate = x0 + n，前提是 x0 + n < p
```

无需计算 `Z^(-1)`，可以直接检查：

```text
X == x_candidate * Z^2 mod p
```

### 实现设计

1. Shamir 主循环返回或保留 Jacobian `(X, Y, Z)`，不立即转为仿射坐标。
2. 若 `Z == 0`，直接返回验签失败。
3. 计算 `x0 = (r - e) mod n`。
4. 计算一次 `Z2 = Z^2 mod p`。
5. 检查 `X == x0 * Z2 mod p`。
6. 若不相等且 `x0 + n < p`，检查 `X == (x0 + n) * Z2 mod p`。
7. 两个候选均不匹配时返回失败。
8. 不再计算仿射 `y`，也不再执行模逆。

### 预期收益

最终阶段预计从约：

```text
297S + 20M
```

降至约：

```text
1S + 1~2M
```

预计整体验签收益为 9%–12%，单签延迟有机会从约 53.6 us/op 降至 48–49 us/op，与 BC 的差距缩小到约 4%–7%。

### 必须覆盖的边界

- `Z == 0`。
- `x0 == 0`。
- `x0 + n == p`。
- `x0 + n < p` 和 `x0 + n >= p`。
- 256 位加法产生进位。
- `r/s` 位于 `1`、`n-1` 等边界。
- 有效签名、篡改签名和随机无效签名。
- 新的项目坐标判断与旧仿射判断结果完全一致。

### 验收门槛

- 热路径至少提升 7%。
- 首次调用和轮换公钥不得回退超过 2%。
- 百万组项目坐标比较差分测试通过。
- 所有合法/非法签名结果与 BC 一致。

## 3.3 Phase 2：实现专用 Montgomery 平方

当前 `mont_sqr(a)` 直接调用 `mont_mul(a, a)`，没有利用平方的对称结构。

普通四肢乘法需要处理 16 个 `64x64` 乘积；平方只存在：

- 4 个对角乘积。
- 6 个非对角乘积，每项乘 2。
- 总计 10 个独立乘积。

### 实施内容

1. 为 portable generic 后端实现专用平方。
2. 为 BMI2 后端实现专用平方。
3. 复用现有 SM2 素数特化 Montgomery 归约。
4. 保证输入输出别名安全，例如 `mont_sqr(a, a)`。
5. 将运行时后端分派同时扩展到乘法和平方。

### 收益来源

当前点公式的主要运算量大致为：

| 点操作 | 域平方 | 域乘法 |
|---|---:|---:|
| Jacobian 倍点 | 4S | 4M |
| Jacobian/仿射混合加 | 3S | 8M |

即使 Phase 1 已消除最终模逆，Shamir 主循环仍执行大量域平方。专用平方预计可使单次平方提升约 20%–35%，整体验签再提升约 5%–10%。

### 预期结果

与 Phase 1 叠加后，验签有机会达到 44–47 us/op，接近、持平或略快于当前 BC。

### 验收门槛

- 至少 1,000,000 组随机平方与通用乘法 `a*a` 差分一致。
- 覆盖 `0`、`1`、`p-1`、最高位全 1、交替位型和别名输入。
- 热验签提升至少 4%。
- 不允许 generic、BMI2 或非 ADX CPU 路径出现正确性差异。

## 3.4 Phase 3：将运行时分派提升到完整验签后端入口

当前每次域乘都通过函数指针调用运行时选择的 Montgomery 实现。一次验签包含上千次域运算，间接调用可能限制编译器进行：

- 跨函数内联。
- 常量传播。
- 长生命周期寄存器分配。
- 点公式级指令调度。

### 候选结构

构建完整、静态绑定的后端：

| 后端 | 内部绑定 |
|---|---|
| generic verify core | generic multiply + generic square |
| BMI2 verify core | BMI2 multiply + BMI2 square |
| 可选汇编 verify core | 整段汇编域运算或点运算 |

CPU 特性只在 `nativeVerifyCore` 入口判断一次，随后调用对应的完整 Shamir 后端，不在每次域运算时通过函数指针分派。

### 风险

此前强制内联完整 CIOS 和绕过函数指针的局部实验曾造成约 2.5%–9.4% 的回退，主要原因可能是代码尺寸增加、寄存器溢出和 I-cache 压力。因此本阶段只能在反汇编和硬件计数器证明间接调用是实际瓶颈后进行。

### 验收门槛

- 热路径提升至少 3%。
- 指令数或 cycles/op 有明确下降。
- 代码体积增长不能造成 I-cache miss 明显上升。
- 若收益不足 3%，直接放弃，不以理论上的“少一次间接调用”为理由合入。

## 3.5 Phase 4：重新评估点坐标与点公式

专用平方实现后，平方和乘法的相对成本会变化，应重新比较点公式，而不是沿用当前成本模型。

### 候选方案

- 当前 `4S + 4M` Jacobian 倍点。
- 针对 `a = -3` 的 `5S + 3M` 倍点公式。
- XYZZ 坐标。
- Chudnovsky Jacobian 坐标。
- 缓存 `Z^2/Z^3` 的混合加法。
- 验签专用、最终只需要 `(X,Z)` 的数据流裁剪。

### 评估方法

不能只比较公式中的 `S/M` 数量，必须测完整 Shamir 循环，并观察：

- 寄存器溢出。
- 栈读写。
- 临时数组复制。
- 分支数量。
- 指令 cache。
- 最终项目坐标比较的衔接成本。

### 验收门槛

- 热单签提升至少 5%。
- 首次调用和多公钥轮换回退不超过 3%。
- 覆盖相等点、互逆点、无穷远点和异常中间状态。

## 3.6 Phase 5：显式预计算验签上下文

当前 native 公钥表是单条目的线程本地缓存。它对同线程单公钥有效，但以下场景仍会重复建立表：

- 多线程处理同一公钥。
- 线程池发生任务迁移。
- 多租户轮换多个公钥。
- 同一业务对象跨线程复用。

### 建议 API 形态

提供不可变的验签上下文，预先保存：

- ID。
- ZA。
- 已完成规范性和曲线方程校验的公钥。
- 公钥 Montgomery 坐标。
- 公钥 16 项奇数倍预计算表。
- 可选的 native 只读句柄。

上下文必须可安全跨线程共享，生命周期由调用方明确管理。

### 预期收益

| 场景 | 预计收益 |
|---|---:|
| 单公钥热路径 | 0%–2% |
| 多线程首次使用 | 明显降低预计算抖动 |
| 多公钥轮换 | 15%–22% |

每个上下文预计占用约 1–2 KB。优先使用显式上下文，不建议在库内部加入无界或难以控制生命周期的隐式 LRU 缓存。

## 3.7 Phase 6：面向吞吐的 SIMD 多消息验签

如果业务目标是每秒验签量，而不是单条 P99 延迟，可将多条独立验签映射到 SIMD lane：

- AVX2 同时处理 4 条验签。
- AVX-512 同时处理 8 条验签。
- 每个 lane 维护独立的 `(X,Y,Z)`、标量和签名状态。
- 相同公钥可共享只读预计算表。
- native 内部不创建业务线程，线程并行由调用方控制。

### 预期收益

- 批量吞吐提升约 1.8x–3x。
- 单签延迟通常不会下降。
- batch 越大，SIMD 利用率越高。

### 不采用随机聚合批验签

SM2 验签包含横坐标取模和比较，不是简单的线性签名方程。除非有严格的批验证正确性与安全性证明，否则不采用 Pippenger 加随机线性组合直接替代逐签验证。

## 4. 明确不再投入的方向

以下方向已经过实际验证，不应在缺少新证据时重复投入：

### 4.1 默认窗口扩大到 8/7 或更高

实测 `8/7` 相比最终 `7/6`：

- 同公钥热收益仅约 0.37%，低于稳定噪声。
- 首次调用慢约 19.2%。
- 强制 cache miss 慢约 4%。
- 轮换公钥慢约 2.7%–7.4%。

因此默认窗口保持 `7/6`。更大窗口只有在显式、长期复用的预计算上下文中才值得重新评估。

### 4.2 当前形式的局部 ADX 重写

修正后的显式 `adcx/adox` Montgomery 实现虽然通过差分测试，但比当前 BMI2 + `__int128` 路径慢约 8.8%–10%，不启用。

只有经过成熟项目验证的完整汇编内核，或者整段点运算汇编，才值得重新评估；不再手工替换局部进位链。

### 4.3 强制内联完整 CIOS

此前实验使 Shamir 约慢 9.4%，说明代码体积和寄存器压力抵消了调用开销，不再作为独立优化方向。

### 4.4 Java 端对象和摘要微优化

默认 native 路径约 99% 时间位于 `nativeVerifyCore`。继续复用 Java `SM3Digest`、减少小数组或调整 ZA 代码，预计收益低于 1%，不应优先投入。

### 4.5 GLV/GLS 标量分解

SM2 曲线参数不具备 secp256k1 可直接利用的高效自同态，不能照搬 GLV 分解。

### 4.6 GPU 单签验签

GPU 调度和数据传输延迟不适合单签低延迟目标，仅可能用于超大规模离线批处理，不进入当前路线。

## 5. 测试与验证方案

## 5.1 正确性测试

每个阶段必须覆盖：

- 国密标准或固定 SM2 签名向量。
- gm-java 签名、gm-java 验签。
- BC 签名、gm-java 验签。
- gm-java 签名、BC 验签。
- 空消息和不同长度消息。
- 默认 ID、空 ID、最大 8191 字节 ID。
- `r/s = 0`、`n`、`n+1`。
- 非规范公钥坐标、离曲线点和零点。
- 篡改消息、篡改签名、篡改公钥。
- native 路径和 Java fallback 结果一致。

## 5.2 native 差分测试

最低要求：

| 测试 | 数量 |
|---|---:|
| Montgomery 乘法随机差分 | 1,000,000 组以上 |
| Montgomery 平方随机差分 | 1,000,000 组以上 |
| Shamir generic/BMI2 差分 | 10,000 组以上 |
| 新项目坐标比较与旧仿射比较差分 | 1,000,000 组以上 |
| 输入输出别名测试 | 所有底层运算均覆盖 |

边界值包括：

```text
0
1
p - 1
n - 1
全 0
全 1
最高位为 1
0xAA/0x55 交替位型
产生 64 位及 256 位进位的构造值
```

## 5.3 性能测试

每项结果至少满足：

- 独立 JVM fork。
- 每个 fork 完成充分预热。
- 至少 7 个测量样本，报告中位数、最小值和最大值。
- 固定 CPU 电源模式，避免后台高负载。
- 同时报告热路径、首次调用、强制 miss 和多公钥轮换。
- 同时报告完整 Java API 和纯 native core。
- BC 使用相同消息、相同 ID、raw64 编码，并在计时前初始化后复用。

不以单轮最好成绩作为结论，不接受低于噪声区间的优化结果。

## 5.4 跨平台矩阵

| 平台 | 最低验证 |
|---|---|
| Windows x64 | JDK 8/21/25，generic/BMI2 |
| Linux x64 | JDK 8/17/21，generic/BMI2 |
| macOS Intel | JDK 17/21，portable x64 |
| macOS Apple Silicon | JDK 17/21，AArch64 |
| Linux ARM64 | JDK 17/21，portable AArch64 |

所有发布用 `.dll`、`.so` 和 `.dylib` 必须由最终同一提交的 `native_mul.c` 重新构建，禁止只更新源码或单一平台二进制。

## 6. 风险控制与回滚

### 6.1 正确性风险

项目坐标直接比较涉及 `mod p` 和 `mod n` 两个域，最容易出现候选范围、加法进位和规范编码错误。必须保留旧仿射实现作为测试 oracle，直到百万差分通过。

### 6.2 性能风险

更少的理论域运算不一定更快。寄存器溢出、代码尺寸、间接调用、分支和 cache miss 都可能抵消收益。所有选择以完整验签端到端数据为准。

### 6.3 平台风险

BMI2、ADX 和 AArch64 后端不能假设行为相同。每个后端必须有独立 differential test，运行时特性检测失败时必须安全回退到 portable 实现。

### 6.4 回滚策略

- 每个 Phase 独立提交。
- 保留可单独开关的新后端，完成稳定验证后再设为默认。
- 出现任何结果不一致时立即回退至当前 `7/6 + BMI2/__int128` 基线。
- 不在同一个提交中同时更换项目坐标比较、平方内核和点公式，以保证问题可归因。

## 7. 推荐实施顺序

| 阶段 | 内容 | 预期收益 | 预计工作量 | 决策 |
|---|---|---:|---:|---|
| Phase 0 | Profiling、反汇编、稳定基线 | 无直接收益 | 1–2 天 | 必做 |
| Phase 1 | Jacobian 项目坐标直接比较，取消模逆和仿射 y | 9%–12% | 2–3 天 | 第一优先级 |
| Phase 2 | 专用 Montgomery 平方 | 5%–10% | 4–7 天 | 第二优先级 |
| Phase 3 | 完整后端入口分派 | 3%–5% | 3–5 天 | 有证据再做 |
| Phase 4 | 点坐标与公式筛选 | 3%–8% | 约 1 周 | Phase 2 后评估 |
| Phase 5 | 显式预计算验签上下文 | 多公钥 15%–22% | 3–5 天 | 面向服务端场景 |
| Phase 6 | AVX2/AVX-512 多消息验签 | 吞吐 1.8x–3x | 2–4 周 | 面向批量吞吐 |

## 8. 最终建议

下一步只实施 Phase 0 和 Phase 1：先建立可归因的性能数据，再用 Jacobian 项目坐标直接完成验签比较，消除最终模逆和无用的仿射 `y` 计算。

这个方向具备以下优势：

- 不增加公钥预计算表。
- 不依赖新的 CPU 指令集。
- 不牺牲首次调用或多公钥轮换场景。
- 可同时惠及 generic、BMI2、Windows、Linux 和 macOS 后端。
- 从当前运算结构看，最有可能一次性消除大部分与 BC 的验签差距。

只有 Phase 1 达到至少 7% 的稳定端到端收益并通过全部差分测试后，再进入专用 Montgomery 平方阶段。
