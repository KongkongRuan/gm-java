name: Release to Maven Central

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 3.2.3)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # 使用 RELEASE_TOKEN 可以绕过 main 分支保护；未配置时回退到 GITHUB_TOKEN
          token: ${{ secrets.RELEASE_TOKEN || secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Decode Maven Central credentials
        run: |
          # Sonatype Central token 格式：username:password
          echo "MAVEN_CENTRAL_USERNAME=$(echo '${{ secrets.MAVEN_CENTRAL_TOKEN }}' | cut -d: -f1)" >> "$GITHUB_ENV"
          echo "MAVEN_CENTRAL_PASSWORD=$(echo '${{ secrets.MAVEN_CENTRAL_TOKEN }}' | cut -d: -f2-)" >> "$GITHUB_ENV"

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
          server-id: release
          server-username: MAVEN_CENTRAL_USERNAME
          server-password: MAVEN_CENTRAL_PASSWORD
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: GPG_PASSPHRASE

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Update version in pom.xml
        run: mvn -q versions:set -DnewVersion=${{ inputs.version }}

      - name: Update version in README.md
        run: |
          sed -i "s/<version>[0-9]\+\.[0-9]\+\.[0-9]\+</<version>${{ inputs.version }}</g" README.md

      - name: Update CHANGELOG.md
        env:
          RELEASE_VERSION: ${{ inputs.version }}
        run: |
          python3 <<'PY'
          import re, os
          version = os.environ['RELEASE_VERSION']
          with open('CHANGELOG.md', encoding='utf-8') as f:
              text = f.read()
          # 提取 "## 未发布" 到下一个 "##" 之间的内容
          pattern = r'^## 未发布\s*\n(.*?)((?=^## )|\Z)'
          m = re.search(pattern, text, re.DOTALL | re.MULTILINE)
          if m:
              unreleased = m.group(1).strip()
              # 如果只有占位文本，则视为无实际内容
              if unreleased == '暂无未发布内容':
                  unreleased = ''
              # 构建新版本区域，并在顶部保留空的 "## 未发布"
              replacement = f'## 未发布\n\n暂无未发布内容\n\n## {version} 更新内容\n'
              if unreleased:
                  replacement += f'{unreleased}\n'
              text = re.sub(pattern, replacement, text, count=1, flags=re.DOTALL | re.MULTILINE)
          with open('CHANGELOG.md', 'w', encoding='utf-8') as f:
              f.write(text)
          PY

      - name: Build and deploy to Maven Central
        run: mvn -P release clean deploy
        env:
          MAVEN_CENTRAL_USERNAME: ${{ env.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ env.MAVEN_CENTRAL_PASSWORD }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}

      - name: Commit version changes
        run: |
          git add pom.xml README.md CHANGELOG.md
          git commit -m "release: v${{ inputs.version }}"

      - name: Create Git tag
        run: git tag v${{ inputs.version }}

      - name: Push changes and tag
        run: git push origin main --tags

      - name: Extract release notes
        env:
          RELEASE_VERSION: ${{ inputs.version }}
        run: |
          python3 <<'PY'
          import re, os
          version = os.environ['RELEASE_VERSION']
          with open('CHANGELOG.md', encoding='utf-8') as f:
              text = f.read()
          pattern = rf'^## {re.escape(version)} 更新内容\s*\n(.*?)(?=^## |\Z)'
          m = re.search(pattern, text, re.DOTALL | re.MULTILINE)
          notes = m.group(1).strip() if m else f'Release v{version}'
          with open('release-notes.md', 'w', encoding='utf-8') as f:
              f.write(notes)
          PY

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "v${{ inputs.version }}" \
            --title "v${{ inputs.version }}" \
            --notes-file release-notes.md \
            target/gm-java-${{ inputs.version }}.jar \
            target/gm-java-${{ inputs.version }}-sources.jar \
            target/gm-java-${{ inputs.version }}-javadoc.jar
